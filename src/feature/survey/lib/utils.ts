import { Survey } from "@/lib/get-data";
import { Control } from "react-hook-form";

export type FormSurveyControl = Control<Survey, any>;

export const no_new_weapons = 0;
export const no_new_units = 0;
