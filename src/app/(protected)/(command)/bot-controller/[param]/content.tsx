"use client";

import { HouseAssets, HouseSettings } from "@/lib/get-data";

const Content = ({
  assets,
  config,
}: {
  assets?: HouseAssets;
  config: HouseSettings;
}) => {
  return (
    <div className="flex justify-center items-center h-screen w-full">
      Comming soon
    </div>
  );
};
export default Content;
